# auth-service

Serviço responsável por autenticação: registro, login, emissão de JWT.

Port: 3001

Como rodar:

1. Copie `.env.example` para `.env` e preencha variáveis.
2. npm install
3. npm run dev
